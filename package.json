{
    "name": "@bsr-comm/server",
    "version": "0.0.1",
    "description": "server",
    "scripts": {
        "build": "tsc",
        "start": "npm run build && node build/server.js",
        "dev": "CORS_ORIGIN=* HOST=localhost PORT=3000 ./node_modules/nodemon/bin/nodemon.js -e ts  --exec \"npm run start\""
    },
    "devDependencies": {
        "socket.io": "^4.1.2",
        "@bsr-comms/utils": "github:iambriansreed/comms-utils",
        "@types/node": "^20.4.8",
        "http-server": "^14.1.1",
        "nodemon": "^3.0.1",
        "typescript": "^5.1.6"
    }
}
